// Debe coincidir con `basePath` en next.config.ts.
// next/image no antepone basePath a `src` automáticamente (a diferencia de next/link),
// por lo que las imágenes estáticas referenciadas por ruta absoluta deben usar este prefijo.
export const BASE_PATH = '/sergestiona';
